apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: knutt-vs-muschel

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: knutt-vs-muschel
  app.kubernetes.io/part-of: knutt-vs-muschel

images:
  - name: ghcr.io/intersales/knutt-vs-muschel-web
    newTag: latest
